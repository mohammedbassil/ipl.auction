Place custom player headshots here to override CDN images.

- Filename for Trent Boult: `trent_boult.jpg`
- Example path (relative to the site root): `player_photos/trent_boult.jpg`

Instructions:
1. Save the attached image as `trent_boult.jpg`.
2. Put the file in `publish/player_photos/` so the web path is `player_photos/trent_boult.jpg`.
3. Reload the app (or browser) — the local image will be used before CDN fallbacks.

Notes:
- Use `.jpg` or `.png`. If you use a different name, update the `LOCAL_PLAYER_PHOTOS` map in `app.js`.
- Filenames are case-sensitive on some servers; keep the name exactly `trent_boult.jpg`.
